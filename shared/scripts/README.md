# Shared Scripts

Shared script assets live here as the long-term stable location for the central toolkit.
The existing top-level `scripts/` and `runtime/scripts/` directories remain the current compatibility sources used by the CLI and installer.
