# Shared Templates

Shared template assets live here as the long-term stable location for the central toolkit.
The existing top-level `templates/` directory remains the current compatibility source used by `init-project.mjs`.
